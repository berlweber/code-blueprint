<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>New Project</title>
</head>
<body>
    <%- include('../partials/_authNavbar.ejs') %>
    <%- include('../partials/_projectsNavbar.ejs') %>
    <main>
        <%- include('../partials/_docsNavbar.ejs') %>
        <h1>Edit Project: <%= project.name %></h1>
        <div class="form">
            <form id="editProject" action="/projects/<%= project._id %>?_method=PUT" method="POST">
                <label for="name">Name:</label>
                <input type="text" name="name" id="name" value="<%= project.name %>" required>

                <label for="discription">Discription:</label>
                <textarea name="discription" 
                rows="15" 
                cols="70"
                id="discription">
                <%= project.discription %>
                </textarea>
            </form>
            <div class="form-buttons">
                <button id="submit" type="submit" form="editProject">Submit</button>
                <a href="/projects/<%= project._id %>" class="btn">Cancel</a>
            </div>
        </div>
    </main>
</body>
</html>